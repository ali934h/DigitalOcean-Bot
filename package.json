{
	"name": "telegram-do-bot",
	"version": "2.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"clean-deploy": "rm -rf .wrangler && wrangler deploy",
		"redeploy": "git pull && rm -rf .wrangler && wrangler deploy"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^4.52.1"
	}
}
