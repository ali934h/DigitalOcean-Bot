{
	"name": "telegram-do-bot",
	"version": "2.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"setup-secrets": "echo 'Setting up secrets...' && npx wrangler secret put TELEGRAM_BOT_TOKEN && npx wrangler secret put ALLOWED_USER_IDS && echo 'Secrets configured!'",
		"check-secrets": "npx wrangler secret list"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^4.52.1"
	}
}
